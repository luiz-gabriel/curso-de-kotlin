fun main()
{
   /** Variaveis mutaveis são variaveis que uma vez inicializada
   * pode ser alterada. Ex:
   * var nome: String = "Luiz"; 
   * nome = "Gabriel";
   *
   * Variaveis imutaveis não podem receber um valor, uma vez 
   * que já recebeu.
   
   * Ex:
   * val idade: Int = 11;
   * idade = 12;
   *
   * Esse exemplo gera o seguinte erro:
   * error: val cannot be reassigned
   * idade = 12;
   * ^
   *
   * Forma diferente de declara um val, definindo o tipo da variavel. 
   * Pois assim o kotlin sabe qual o tamanho dá variavel.
   * val x: Int
   * x = 1;
   */

   
}